export interface LoginType {
  email: string;
  password: string;
}

export interface UserRegisterType {
  email: string;
  phoneNumber: string;
  password: string;
  companyName: string;
  role: string;
  address: string;
}
