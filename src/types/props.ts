export interface HeadingType {
  title: string;
  subtitle: string;
}

export interface ProductDetailsType {
  spaceType: string;
  location: string;
  icon: any;
  reviews: string;
  description: string;
  assets: {
    name: string;
  };
}
